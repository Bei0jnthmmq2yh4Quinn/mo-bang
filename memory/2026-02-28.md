# 2026-02-28

### [PROJECT:OpenClaw] CLIProxy 403 修复 + 模型配置大扫除
- **结论**: cliproxy 所有模型经 OpenClaw 调用链报 403（直连 curl 可用），根因是 SDK 请求头触发上游风控。通过部署本地代理 `cliproxy_proxy.py`（端口 19091）清洗请求头修复。
- **文件变更**: `scripts/cliproxy_proxy.py`; `~/.config/systemd/user/cliproxy-proxy.service`; `~/.openclaw/openclaw.json`（cliproxy.baseUrl → `http://127.0.0.1:19091/v1`）; `~/.openclaw/agents/main/agent/models.json`（同步 baseUrl）
- **教训**: 同 freestyle 修复——新渠道接入必须测"OpenClaw 真实调用链"，不能只测 curl 直连
- **标签**: #cliproxy #proxy #403 #openclaw

### [PROJECT:OpenClaw] 35 个 session 模型覆盖重置
- **结论**: 所有 session（主私聊、群聊、topic、cron、subagent）的模型覆盖全部清除，统一回默认 `003636/gpt-5.2-codex`
- **操作**: 直接编辑 `sessions.json` 删除所有 `model` 字段
- **标签**: #openclaw #model-reset

### [PROJECT:OpenClaw] 新增模型切换别名
- **结论**: 配好 3 个高频别名供老板快速切换
- **别名表**: `52` → gpt-5.2-codex, `53` → gpt-5.3-codex, `think` → cliproxy/claude-opus-4-6-thinking
- **用法**: 在 Telegram 发 `/model 52` `/model 53` `/model think` `/model default`
- **标签**: #openclaw #aliases #config

### [PROJECT:OpenClaw] 记忆文件完整性确认
- **结论**: memory/ 目录 30 个文件全部完好，无截断/压缩迹象。群聊的"压缩"是 session transcript compaction，不影响 memory 文件。
- **标签**: #openclaw #memory #verification
